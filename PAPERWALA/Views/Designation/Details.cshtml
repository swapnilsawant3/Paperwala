@model IEnumerable<PAPERWALA.Models.DesignationDTO>

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_LayoutAdminMain.cshtml";
}

<div class="page-header">
    <div class="row align-items-end">
        <div class="col-lg-8">
            <div class="page-header-title">
                <i class="ik ik-layers bg-red"></i>
                <div class="d-inline">
                    <h5>Designation Name List</h5>
                    <span>Here you can see all Designation Names</span>
                </div>
            </div>
        </div>

    </div>
</div>
<div class="row">
    <div class="col-sm-12">
        <div class="card">
            <div class="card-header d-block">
                <h3> Designation Name List  | <span> @Html.ActionLink("Create Designation", "Create", "Designation", new { @class = "btn btn-info btn-default" }) </span></h3>
            </div>
            <div class="card-body">
                <div class="dt-responsive">
                    <table id="simpletable"
                           class="table table-striped table-bordered nowrap">
                        <thead>
                            <tr>
                                <th>Desgnation Name</th>
                                <th>Edit</th>
                                <th>Delete </th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null)
                            {
                                foreach (var item in Model)
                                {
                                    <tr>

                                        <td>@item.Designation</td>
                                        <td>@Html.ActionLink("Edit", "Edit", new { id = @item.DesignationId })</td>
                                        <td>@Html.ActionLink("Delete", "Delete", new { id = @item.DesignationId }, new { onclick = "return confirm('Are you sure you wish to delete this State?');" })</td>
                                    </tr>
                                }

                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

</div>


